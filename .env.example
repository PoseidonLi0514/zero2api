# 必填：所有代理/管理接口的鉴权 Key（x-api-key 或 Authorization: Bearer）
API_KEY=change-me

# 可选：外部映射端口（docker-compose.yml 里会用）
PORT=8787

# 可选：提高/降低最大请求体（多模态 data URL 等场景会很大）
MAX_REQUEST_BODY_BYTES=20971520

# 可选：调试日志（默认关闭）
# DEBUG_MODE=1 会开启基础调试日志（上游请求摘要/非2xx响应体）
# DEBUG_RAW_STREAM=1 会额外打印上游原始 SSE chunk 与 data 事件（日志量较大）
# DEBUG_LOG_MAX_CHARS=4000
